{
  "description": "English prompt improvement (display-only via systemMessage)",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/improve-prompt.ts",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
